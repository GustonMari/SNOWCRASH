go to root and chmod -R 777 * to get access to all 

find / -user flag00 2>/dev/null pertmit to find this:
/usr/sbin/john
/rofs/usr/sbin/john

go to the folder and cat the file:
you will find cdiiddwpgswtgt => decrypt with: https://www.dcode.fr/chiffre-cesar

